"""LEVI SI Team — the four mind-roles of the synthetic intelligence team.

Levi (the voice — lead), Alpha (first mind — reasoning), Omega
(judge/evaluator — the culmination), Dweller (labor — the SI that dwells in the depths of the work).

Layout:
  charter.py   — RoleCharter dataclass (the binding crew contract)
  roles.py     — the four crew charters
  substrate.py — substrate router: registered probe > levi.alpha > rules
  counsel.py   — consult() receipt flow + capability_report()
  si/          — native SI cores (LEVI-native synthetic reasoning, rules today)
  ai/          — labeled AI-counterpart bridges (conventional-protocol twins)
  cli.py       — `levi si-team` command surface
"""
