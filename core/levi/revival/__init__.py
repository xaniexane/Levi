"""revival: retired-software ideas reborn as original LEVI works.

Defensive, stdlib-only, local-first modules inspired by historical systems —
reimplemented from scratch as LEVI's own code, never copied:

- ``telescript``: capability-bounded agent execution (deny-closed tokens).
- ``plan9``: per-task namespaces + 9P-style local message passing.
- ``arexx``: named application command ports for automation.

Deliberately no package-level exports: import the module you need.
"""
