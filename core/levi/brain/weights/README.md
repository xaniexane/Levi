# tiny brain weights

`tiny-gpt.pt` is produced by `../train/train.py` (CPU, ~2-4M params).
It is git-ignored when large; this README records provenance instead.

After a training run, append:

- sha256: <fill in>
- steps / params / corpus chars: see train_log.json
- held-out loss: see eval.json
- samples: see samples.md
