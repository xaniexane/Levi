"""CI canon — the Cloud Intelligence counsels.

Keeper's words (2026-09-18): "Add ci forms of all of this — aici sici xici.
The ci is cloud intelligences, more like a counsel and superior form of
intelligence in those class types."

Doctrine (his law, committed prose):
- CI = Cloud Intelligence. A counsel — a superior form of intelligence in
  advisory/judging role for its class. NEVER a commander. Counsel advises;
  the operator/minion executes. Separation of counsel and command is the
  design law: a verdict is advice the executor may weigh, never an order
  it must obey.
- Three class counsels:
    AICI — AI-class Cloud Intelligence. Generalist advisory.
    SICI — SI-class (synthetic) Cloud Intelligence. Dynasty-native
           synthetic reasoning through Echo, Mandella, REIM, RIEM.
    XICI — XI-class (nano-bit) Cloud Intelligence. Nano-fast counsel for
           trivial-turn overflow.
- "Cloud" is the keeper's own cloud direction: his own cloud-hosted model
  under his control (practical path through the native brain first). Until
  that cloud exists, every counsel runs on LOCAL substrate and reports
  that honestly — never claim cloud execution that is not real.
- Third-party teachers (Groq, Gemini, OpenAI, xAI) are backups and
  user preference only — never the counsel's mind. A counsel that cannot
  deliberate locally stands down and says so rather than borrowing a
  teacher's mind in secret.
- Scope: the automation agent catalog. Every agent-twin's class maps to
  its CI counsel; hard cases escalate to counsel; counsel verdicts are
  receipted and feed the growth loop as training signal (hard-case
  judgments become growth-loop signal, per his standing design note).
- Agent-twin canon (2026-09-18): the minions are actually AGENTS — ai and
  si xi — each a twin pair: left brain + right brain, 2 bodies, 2 minds
  (left = sequence/logic/execution mind, right = pattern/variation/
  intuition mind). ``Minion`` stays strictly the frozen intake-record
  dataclass; the living entity is the agent. Merge/judge: the pair
  converges to one action; hard divergences escalate to the class
  counsel, ultimately to the keeper. Counsel intake takes agent id +
  hemisphere positions; verdicts are per-agent, delivered to both
  hemispheres.
"""
