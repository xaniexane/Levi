<?xml version="1.0" encoding="utf-8"?>
<!--
    Cleartext (non-TLS) traffic is DENIED by default.
    The local development backends (OAuth :8080, messaging :8081) run over
    cleartext HTTP; they are allow-listed here for the emulator/devices on
    the local network ONLY. Production builds must point at https:// hosts,
    which are covered by the base-config (no exception needed).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator loopback to the host machine -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
