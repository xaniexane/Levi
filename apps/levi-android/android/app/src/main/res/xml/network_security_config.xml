<?xml version="1.0" encoding="utf-8"?>
<!--
    Cleartext (non-TLS) traffic is DENIED by default (see AndroidManifest
    usesCleartextTraffic="false"). The LEVI server URL is user-configured;
    cleartext is allow-listed ONLY for loopback hosts (on-device Termux
    server, emulator host loopback). LAN / public servers must use HTTPS —
    ServerConfig.normalize() rejects http:// for non-local hosts so the
    failure is legible instead of a silent WebView error.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator loopback to the host machine -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
