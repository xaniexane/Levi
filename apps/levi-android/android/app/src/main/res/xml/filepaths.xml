<?xml version="1.0" encoding="utf-8"?>
<!-- FileProvider paths: the cache dir is shared (data exports, issue
     reports). The app files dir is shared read-only for the "open_file"
     device tool — still strictly inside the app sandbox; the tool itself
     refuses any path outside the app's own storage. -->
<paths>
    <cache-path name="shared" path="." />
    <files-path name="app_files" path="." />
</paths>
