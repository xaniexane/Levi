<?xml version="1.0" encoding="utf-8"?>
<!-- FileProvider paths: only the cache dir is ever shared (data exports,
     issue reports). Nothing else leaves the app sandbox this way. -->
<paths>
    <cache-path name="shared" path="." />
</paths>
