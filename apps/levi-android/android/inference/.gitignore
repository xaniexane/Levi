# Fetched at build time (pinned commit in native/build-android.sh) — never committed.
src/main/cpp/llama.cpp/
# Built by native/build-android.sh — never committed; CI rebuilds them.
src/main/jniLibs/
native/build/
